
<div class="navbar navbar-default navbar-fixed-top header" role="navigation">
    <div class="container">
        <div class="navbar-header">
            <a href="{{ path('homepage') }}"> <img src="{{ asset('img/logo.png') }}" class="navbar-brand logo"></a>
            <a href="{{ path('homepage') }}" class="navbar-brand"><h5>(c) christian.toinard@insa-cvl.fr</h5></a>
        </div>
        <div class="collapse navbar-collapse">

            <ul class="nav navbar-nav navbar-right">
                {% if not is_granted('ROLE_ADMIN') and not is_granted('ROLE_SECRETARY')%}
                    <li>
                        <a href="{{ path('notifications') }}" class="dropdown-toggle">
                            <span class="glyphicon glyphicon-bell"></span>
                            <strong>Notifications</strong>
                        </a>
                    </li>
                {% endif %}

                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                        <span class="glyphicon glyphicon-user"></span>
                        <strong>{{ app.user.username }}</strong>
                        <span class="glyphicon glyphicon-chevron-down"></span>
                    </a>
                    <ul class="dropdown-menu">
                        <li>
                            <div class="navbar-login">
                                <div class="row">
                                    <div class="col-lg-4">
                                        <p class="text-center">
                                            <span class="glyphicon glyphicon-user icon-size"></span>
                                        </p>
                                    </div>
                                    <div class="col-lg-8">
                                        <p class="text-left"><strong>{{ app.user.username }}</strong></p>
                                        <p class="text-left">
                                            <a href="mailto:christian.toinard@insa-cvl.fr?subject=Application locale - MesureLocal&body=Bonjour"
                                                class="btn btn-default btn-block btn-sm">
                                                Rapporter un bogue
                                                <span class="glyphicon glyphicon-bell"></span>
                                            </a>
                                            <!-- Every one except the admin can change their password using this page -->
                                            {% if not is_granted('ROLE_ADMIN')%}
                                                <a href="{{ path('changepassword') }}"
                                                    class="btn btn-default btn-block btn-sm">
                                                    Changer le mot de passe
                                                    <span class="glyphicon glyphicon-lock"></span>
                                                </a>
                                            {% endif %}

                                            {% if is_granted('ROLE_MEDICAL') or is_granted('ROLE_PARAMEDICAL') %}
                                                <a href="{{ path(('confignotifications')) }}" class="btn btn-default btn-block btn-sm">
                                                    Gérer les notifications
                                                    <span class="glyphicon glyphicon-bell"></span>
                                                </a>
                                            {% endif %}

                                            {% if is_granted('ROLE_SECRETARY') %}
                                                <a href="{{ path(('importExistent')) }}" class="btn btn-default btn-block btn-sm">
                                                    Importer des dossiers
                                                    <span class="glyphicon glyphicon-download"></span>
                                                </a>
                                            {% endif %}

                                            <a href="{{ logout_path('main') }}"
                                                class="btn btn-danger btn-block btn-sm">
                                                Déconnexion
                                                <span class="glyphicon glyphicon-off"></span>
                                            </a>
                                        </p>
                                    </div>
                                </div>
                            </div>
                        </li>
                    </ul>
                </li>
            </ul>
        </div>
    </div>
</div>