<!DOCTYPE html>
<html>
<head>

    <meta charset="UTF-8" />
    <title>{% block title %}MesureLocal : Base de données locale{% endblock %}</title>

    <link rel="stylesheet" href="{{ asset('css/bootstrap.min.css') }}">
    <link rel="stylesheet" href="{{ asset('css/login.css') }}" />


    {% block stylesheets %}{% endblock %}
    <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
</head>

<body>

<H2>MesureLocal (c) Pour toute utilisation, copie ou modification de ce programme vous devez obtenir préalablement l'autorisation auprès de christian.toinard@insa-cvl.fr</H2>

    <div class="card card-container">
        <img id="profile-img" class="profile-img-card" src="{{ asset('img/logo-blue.png') }}" />
        <p id="profile-name" class="profile-name-card"></p>

        <form class="form-signin" action="{{ path('login') }}" method="post">

            {% if error %}
                <div class="alert alert-danger" role="alert">
                    Nom d'utilisateur et/ou mot de passe incorrect(s)
                </div>
            {% endif %}

            <span id="reauth-email" class="reauth-email"></span>

            <label for="_username" class="sr-only">Username</label>
            <input type="text"
                   id="_username"
                   name="_username"
                   class="form-control"
                   placeholder="Nom d'utilisateur"
                   required autofocus>

            <label for="_password" class="sr-only">Password</label>
            <input type="password"
                   id="_password"
                   name="_password"
                   class="form-control"
                   placeholder="Mot de passe"
                   required>

            <button class="btn btn-lg btn-primary btn-block btn-signin" type="submit">Connexion</button>
        </form>
    </div>


</body>

